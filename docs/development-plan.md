# PaperMind 七阶段开发计划

本文档用于固定项目推进口径，后续每个阶段完成后都需要测试、提交并推送到 GitHub。

## 阶段一：需求和产品设计

状态：部分完成。

已完成：

- README 初版
- 核心页面已在产品中落地：文档库、上传区、阅读页、AI 问答区、摘要和提纲面板
- 第一版功能边界已明确：PDF/Markdown 上传、文本解析、文档切分、基于文档问答、引用原文片段、自动摘要

待补齐：

- 功能清单文档
- 页面流程文档
- 数据结构设计文档
- 简单交互原型说明

## 阶段二：搭建全栈项目骨架

状态：基本完成。

已完成：

- Next.js 前端项目
- FastAPI AI 服务
- 健康检查接口
- 前后端联调
- 环境变量管理
- Docker Compose 初版
- 本地 JSON 存储初始化

待补齐：

- `packages/shared` 共享类型包
- 统一异常返回格式
- 更完整的基础日志
- 后续数据库初始化迁移

## 阶段三：实现文档上传和解析

状态：基本完成。

已完成：

- PDF、Markdown、TXT 上传
- 文件类型和大小校验
- 文件保存和元数据保存
- PDF 和文本解析
- 文档切分
- 处理状态、失败原因、页数、文件大小展示

待补齐：

- 重复上传检测
- `processing_jobs` 处理任务表
- 更细的解析失败分类

## 阶段四：实现 RAG 问答核心

状态：基本完成。

已完成：

- 文档切分
- 本地向量化索引
- 关键词和向量相似度混合检索
- 根据用户问题检索相关片段
- 拼接引用上下文
- 调用 DeepSeek 模型
- 流式返回答案
- 展示引用来源
- 找不到依据或模型不可用时使用抽取式兜底回答
- 单文档多轮对话历史
- 历史消息作为模型上下文传入
- 固定问答输出结构：直接回答、文档依据、边界和不确定性
- 对话历史过长时自动裁剪最近消息，并压缩较早历史为摘要

待补齐：

- 可替换的真实 Embedding 模型
- 外部向量数据库
- 更强的语义召回评估和检索调参

## 阶段五：增加智能文档功能

状态：基本完成。

已完成：

- 文档摘要
- 短摘要和详细摘要切换
- 关键词
- 文章提纲
- 建议追问
- 用户手写笔记保存
- Markdown 阅读笔记导出
- 模型结构化输出
- JSON Schema 校验

待补齐：

- 更细的洞察缓存策略
- 模型洞察失败原因前端展示

## 阶段六：补齐工程化能力

状态：基本完成。

已完成：

- 模型请求超时和重试
- 请求日志和错误日志
- 文件大小和请求频率限制
- 统一异常兜底
- 模型服务不可用时的错误提示
- 删除文档时同步删除向量数据

计划内容：

- Redis 异步任务队列
- 文档解析失败自动重试
- 文档处理幂等性
- 问答结果缓存
- Token 和调用费用记录
- Playwright 端到端测试

## 阶段七：增加 Agent 和作品集能力

状态：进行中。

计划内容：

- `summarize_document`
- `extract_keywords`
- `generate_questions`
- `create_markdown_note`
- `export_outline`
- 工具调用系统
- 自动化工作流
- 系统架构图
- 数据库设计图
- 接口文档
- 测试报告
- 简历项目描述

本轮已完成：

- 新增后端 Agent 工具注册表，将 `summarize_document`、`extract_keywords`、`generate_questions`、`create_markdown_note`、`export_outline` 封装为可调度工具。
- 新增统一工具执行接口：`POST /documents/{document_id}/agent-tools/run`。
- 工具结果同时返回 Markdown 内容、结构化 JSON 数据、引用片段和生成时间。
- 阅读页新增 Agent 工具面板，支持一键运行工具、复制结果和下载 Markdown。
- E2E 覆盖上传文档后执行 Agent 工具并展示结果。

## 阶段六本轮更新

已完成：

- 文档处理幂等性：上传内容会计算 SHA-256，重复内容直接返回已有文档。
- 问答结果缓存：同一文档下的独立相同问题会命中缓存，同时保留当前对话历史。
- Token 和调用费用记录：模型日志记录 prompt/completion/total tokens、估算费用和缓存命中标记。
- 文档解析失败自动重试：解析任务会记录到 `processing_jobs`，并按配置记录 attempts、状态和失败原因。
- Redis 异步任务队列：Docker Compose 增加 Redis 和 `ai-worker`，API 可将文档处理任务入队。
- Playwright 端到端测试：覆盖上传文档、阅读页和文档问答主流程。

仍待推进：
- 阶段七后续补充系统架构图、数据库设计图、接口文档、测试报告和简历项目描述。
